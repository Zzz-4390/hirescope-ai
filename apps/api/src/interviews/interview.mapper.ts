import { InterviewStatus } from '@prisma/client';

type InterviewRow = { id: string; title: string; status: InterviewStatus; difficulty: string; questionCount: number; currentIndex: number; failureCode: string | null; failureMessage: string | null; startedAt?: Date | null; submittedAt?: Date | null; completedAt?: Date | null; createdAt: Date; updatedAt: Date };
export function mapInterview(interview: InterviewRow) { const { failureCode, failureMessage, ...fields } = interview; return { ...fields, failure: failureCode ? { code: failureCode, message: failureMessage } : null }; }
export function mapInterviewDetail<T extends InterviewRow & { questions: Array<{ id: string; sequence: number; category: string; difficulty: string; question: string; answer: { content: string; answeredAt: Date; updatedAt: Date } | null }>; _count: { answers: number } }>(interview: T) {
  const { questions, _count, ...base } = interview; const answeredCount = _count.answers; const answerProgress = { answeredCount, questionCount: interview.questionCount, percentage: interview.questionCount ? Math.floor(answeredCount * 100 / interview.questionCount) : 0 }; const response = { ...mapInterview(base), answeredCount, answerProgress }; if (interview.status === InterviewStatus.GENERATING || interview.status === InterviewStatus.FAILED) return response; return { ...response, questions: questions.map(({ id, sequence, category, difficulty, question, answer }) => ({ id, sequence, category, difficulty, question, answer })) };
}
