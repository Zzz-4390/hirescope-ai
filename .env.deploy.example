# Use the exact public Registry domain shown by the Hangzhou ACR console.
# Do not include http://, https://, a namespace path, or a guessed VPC domain.
ACR_PUBLIC_REGISTRY=replace_with_exact_public_registry_domain
ACR_NAMESPACE=hirescope-ai
